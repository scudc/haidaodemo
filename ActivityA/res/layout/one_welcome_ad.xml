<?xml version="1.0" encoding="utf-8"?>
<ScrollView  xmlns:android="http://schemas.android.com/apk/res/android"
	    android:id="@+id/loadingScrollView"
	    android:layout_width="fill_parent"
	    android:background="#FFFFFF"
	    android:layout_height="fill_parent"
	    android:scrollbars="vertical" >

<FrameLayout android:id="@id/welcome_ad_rlayout" android:layout_width="fill_parent" android:layout_height="fill_parent"
  android:background="@color/welcomeBackground">
       <ProgressBar
        android:layout_gravity="center"
        android:visibility="visible"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        style="@style/newProgressBar" android:id="@+id/progressBar">
        
    </ProgressBar>
   
</FrameLayout>
</ScrollView>
